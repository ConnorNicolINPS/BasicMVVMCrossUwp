<views:MvxWindowsPage x:Class="MvvmCrossUWPExample.Views.FirstView"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:views="using:MvvmCross.Uwp.Views"
                      mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="10">
            <TextBox Text="{Binding Hello, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding Hello}" Margin="0,10,0,0"/>
            <Button Content="Move next" Command="{Binding MoveNextView}" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</views:MvxWindowsPage>
